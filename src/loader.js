import React from "react";

/**
 * @method Loader
 */
const Loader = () => (
    <div className="loader">
        <div className="ball-pulse-sync">

        </div>
    </div>
);

export default Loader;